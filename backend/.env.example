# DayCoach Backend — 환경변수

# GitHub Copilot 인증 (선택 — 없으면 로컬 gh auth 사용)
# GITHUB_TOKEN=ghp_your_token_here

# 사용할 모델 (선택 — 없으면 Copilot 기본 모델 사용)
# COPILOT_MODEL=gpt-4o

# 서버 설정 (선택 — 기본값 있음)
PORT=3001
CORS_ORIGIN=*
